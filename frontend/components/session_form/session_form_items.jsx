import React from 'react';

const SessionFormItems = ({ field, className, placeholder, handleInput, onBlur, placeHoldersOnBlur })  => {

    // if (props.placeholdersOnBlur.includes(placeholder)) (

    // ) else (
    
    //     <input className={className} type="text" placeholder="" onChange={this.handleInput(`${field}`)} />

    // )
};

export default SessionFormItems;